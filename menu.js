// Function to change font across the page
function changeFont(font) {
    document.body.style.fontFamily = font;
    alert(`Font changed to ${font}`);
}

// Function to simulate saving data
function saveData() {
    alert("Data has been saved!");
}
